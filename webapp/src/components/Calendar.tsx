import { Box, Typography } from '@mui/material';
import { Link } from 'react-router-dom';
import React from 'react';

const Calendar = () => (
  <Box >
    <Link to=''>go to the calendar</Link>
  </Box>
);

export default Calendar;
