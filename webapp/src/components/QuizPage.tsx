import { Box, Container, Grid } from '@mui/material';
import React, { useState } from 'react';

const QuizPage = () => {
  return (
    <Container fixed>
      <h1>Quiz</h1>
    </Container>
  );
};

export default QuizPage;
