import React from 'react';

const CalendarPage = () => {
  // The goal in this ticket is to wire the appropriate functions to ensure
  // programsList gets filled in with API data so we can render it.
  let programsList;
  return <div>Calendar Page</div>;
};

export default CalendarPage;
